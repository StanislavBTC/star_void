# Дерево структуры проекта Star_Void

```
star_void/
│
├── mailn.py                          # Точка входа в приложение
├── pyproject.toml                     # Конфигурация проекта
├── requirements.txt                   # Зависимости
├── README.md                          # Документация
├── .gitignore                         # Git ignore файл
├── txt.md                             # [?] Неизвестное назначение
│
├── PROJECT_STRUCTURE.md               # Подробное описание структуры
├── TREE.md                            # Этот файл - визуальное дерево
│
├── readme_resource/                   # Ресурсы для README
│   └── logo_name.jpeg                 # Логотип проекта
│
└── src/                               # Исходный код
    │
    ├── ai/                            # Ядро системы ИИ
    │   ├── responder.py               # [TODO] Единая точка общения с ИИ
    │   ├── filters.py                 # [✓] Пост-фильтры ответов
    │   └── silence.py                 # [TODO] Логика молчания
    │
    ├── modules/                       # Режимы работы
    │   ├── ask.py                     # [TODO] Режим диалога
    │   ├── distort.py                 # [TODO] Режим искажения
    │   ├── void.py                    # [TODO] Пассивный режим
    │   └── silence.py                 # [TODO] Режим молчания
    │
    ├── utils/                         # Утилиты
    │   ├── randomness.py              # [✓] Рандомизация промптов
    │   ├── delay.py                   # [TODO] Паузы и медленность
    │   ├── text.py                    # [TODO] Обрезка и фрагментация
    │   └── __pycache__/               # Кэш Python
    │
    └── config/                        # Конфигурация
        ├── config.env                 # Переменные окружения
        │
        └── ai/                        # Промпты для ИИ
            ├── core.txt               # Базовый системный промпт
            ├── ask.txt                # Промпт для режима диалога
            ├── distort.txt            # Промпт для режима искажения
            └── void.txt               # Промпт для пассивного режима
```

---

## Легенда

- `[✓]` - Полностью реализован
- `[TODO]` - Требует реализации
- `[?]` - Неясное назначение

---

## Карта связей

```
mailn.py
    ↓
src/ai/responder.py ←─────────────┐
    ↓                              │
    ├─→ src/utils/randomness.py   │
    │       ↓                      │
    │   src/config/ai/*.txt        │
    │                              │
    ├─→ src/ai/filters.py          │
    │                              │
    ├─→ src/ai/silence.py          │
    │                              │
    └─→ src/modules/               │
            ├─ ask.py      ────────┤
            ├─ distort.py  ────────┤
            ├─ void.py     ────────┤
            └─ silence.py  ────────┘
                ↓
        src/utils/delay.py
        src/utils/text.py
```

---

## Поток данных

```
1. Пользователь → input(">")
                    ↓
2. mailn.py → определяет режим
                    ↓
3. modules/{режим}.py
                    ↓
4. ai/responder.py
                    ↓
   ┌────────────────┴────────────────┐
   │                                 │
   ↓                                 ↓
5. utils/randomness.py          ai/silence.py
   загрузка промпта              проверка молчания
   ↓                                 ↓
6. LLM API вызов                если молчать → None
   ↓                                 
7. ai/filters.py                     
   очистка ответа                    
   ↓                                 
8. utils/text.py                     
   фрагментация (для distort)        
   ↓                                 
9. utils/delay.py                    
   задержка перед выводом            
   ↓                                 
10. print(ответ) или молчание
```

---

## Архитектурные слои

```
┌─────────────────────────────────────────────┐
│           ИНТЕРФЕЙС (mailn.py)              │
│         Терминальное взаимодействие         │
└─────────────────┬───────────────────────────┘
                  │
┌─────────────────┴───────────────────────────┐
│          РЕЖИМЫ (src/modules/)              │
│     ask, distort, void, silence             │
└─────────────────┬───────────────────────────┘
                  │
┌─────────────────┴───────────────────────────┐
│         ЯДРО ИИ (src/ai/)                   │
│   responder, filters, silence               │
└─────────────────┬───────────────────────────┘
                  │
┌─────────────────┴───────────────────────────┐
│        УТИЛИТЫ (src/utils/)                 │
│   randomness, delay, text                   │
└─────────────────┬───────────────────────────┘
                  │
┌─────────────────┴───────────────────────────┐
│       КОНФИГУРАЦИЯ (src/config/)            │
│     config.env, ai/*.txt                    │
└─────────────────────────────────────────────┘
```

---

## Матрица зависимостей

| Модуль           | Зависит от                                    |
|------------------|-----------------------------------------------|
| mailn.py         | ai/responder, modules/*                       |
| ai/responder     | utils/randomness, ai/filters, ai/silence      |
| ai/filters       | (нет зависимостей)                            |
| ai/silence       | utils/randomness (опционально)                |
| modules/ask      | ai/responder, utils/delay                     |
| modules/distort  | ai/responder, utils/text, utils/delay         |
| modules/void     | ai/responder, utils/delay, utils/randomness   |
| modules/silence  | (нет зависимостей)                            |
| utils/randomness | config/ai/*.txt                               |
| utils/delay      | (нет зависимостей)                            |
| utils/text       | (нет зависимостей)                            |

---

## Приоритеты разработки

### 🔴 Критично (блокируют работу)
1. `src/ai/responder.py` - основная логика
2. `src/ai/silence.py` - принятие решений о молчании
3. `mailn.py` - исправить импорты

### 🟡 Важно (нужно для полноты)
4. `src/utils/delay.py` - атмосфера паузы
5. `src/utils/text.py` - искажение текста
6. `src/modules/ask.py` - основной режим
7. `src/modules/distort.py` - режим искажения

### 🟢 Желательно (улучшения)
8. `src/modules/void.py` - фоновый режим
9. `src/modules/silence.py` - явное молчание
10. `.gitignore` - настройка репозитория
11. Тесты и документация

---

## Размер файлов (примерный)

```
mailn.py                 ~50 строк
ai/responder.py          ~150 строк
ai/filters.py            ~70 строк    [✓]
ai/silence.py            ~80 строк
modules/ask.py           ~30 строк
modules/distort.py       ~40 строк
modules/void.py          ~50 строк
modules/silence.py       ~10 строк
utils/randomness.py      ~120 строк   [✓]
utils/delay.py           ~50 строк
utils/text.py            ~80 строк
config/ai/core.txt       ~30 строк    [✓]
config/ai/ask.txt        ~20 строк    [✓]
config/ai/distort.txt    ~30 строк    [✓]
config/ai/void.txt       ~15 строк    [✓]
```

**Итого:** ~825 строк кода

---

## Конфигурационные файлы

```
config.env               API ключи, настройки
pyproject.toml           Метаданные проекта
requirements.txt         Зависимости Python
.gitignore               Игнорируемые файлы
README.md                Документация
PROJECT_STRUCTURE.md     Подробное описание
TREE.md                  Визуальная структура (этот файл)
```
