# Архитектура Star_Void

Визуальное руководство по архитектуре проекта.

---

## 🎯 Концептуальная модель

```
┌─────────────────────────────────────────────────────────────┐
│                         ПОЛЬЗОВАТЕЛЬ                        │
│                    (Терминальный ввод)                      │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                      ИНТЕРФЕЙСНЫЙ СЛОЙ                      │
│                        (mailn.py)                           │
│                                                             │
│  • Чтение ввода                                             │
│  • Обработка команд (/ask, /distort, /void, /silence)       │
│  • Управление режимами                                      │
│  • Вывод результата                                         │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                      СЛОЙ РЕЖИМОВ                           │
│                    (src/modules/)                           │
│                                                             │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐     │
│  │   ask    │  │ distort  │  │   void   │  │ silence  │     │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘     │
│       │             │              │             │          │
│       └─────────────┴──────────────┴─────────────┘          │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                       ЯДРО ИИ                                │
│                     (src/ai/)                                │
│                                                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              RESPONDER (responder.py)               │   │
│  │  • Загрузка промптов                                │   │
│  │  • Решение о молчании                               │   │
│  │  • Вызов LLM API                                    │   │
│  │  • Применение фильтров                              │   │
│  └───────────┬──────────────────┬──────────────────────┘   │
│              │                  │                           │
│              ▼                  ▼                           │
│     ┌────────────────┐  ┌────────────────┐                 │
│     │   SILENCE      │  │    FILTERS     │                 │
│     │  (silence.py)  │  │  (filters.py)  │                 │
│     │                │  │                │                 │
│     │ • Вероятности  │  │ • Советы       │                 │
│     │ • Паттерны     │  │ • Эмпатия      │                 │
│     │ • Режимы       │  │ • Длина        │                 │
│     └────────────────┘  └────────────────┘                 │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                    УТИЛИТЫ (src/utils/)                      │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ RANDOMNESS   │  │    DELAY     │  │     TEXT     │     │
│  │              │  │              │  │              │     │
│  │ • Промпты    │  │ • Паузы      │  │ • Фрагменты  │     │
│  │ • Блоки      │  │ • Печать     │  │ • Слова      │     │
│  │ • Форматы    │  │ • Анимация   │  │ • Редукция   │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│               КОНФИГУРАЦИЯ (src/config/)                     │
│                                                              │
│  ┌────────────────┐         ┌──────────────────────────┐   │
│  │  config.env    │         │    ai/*.txt              │   │
│  │                │         │                          │   │
│  │ • API ключи    │         │ • core.txt  (философия)  │   │
│  │ • Настройки    │         │ • ask.txt   (диалог)     │   │
│  │ • Параметры    │         │ • distort.txt (искажение)│   │
│  │                │         │ • void.txt  (пустота)    │   │
│  └────────────────┘         └──────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                    ВНЕШНИЕ СЕРВИСЫ                           │
│                                                              │
│     ┌─────────┐     ┌──────────┐     ┌─────────────┐       │
│     │ OpenAI  │     │Anthropic │     │    Ollama   │       │
│     │   API   │     │   API    │     │   (Local)   │       │
│     └─────────┘     └──────────┘     └─────────────┘       │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 Поток данных

### Базовый сценарий (режим ask)

```
1. ВВОД ПОЛЬЗОВАТЕЛЯ
   │
   ├─> "Я не знаю, что делать дальше"
   │
   ▼
2. mailn.py
   │
   ├─> Определяет режим: "ask"
   ├─> Вызывает: modules/ask.py
   │
   ▼
3. modules/ask.py
   │
   ├─> Вызывает: ai/responder.respond(input, mode="ask")
   │
   ▼
4. ai/responder.py
   │
   ├─> Проверяет: silence.should_be_silent(input, "ask")
   │   │
   │   ├─> Вероятность 20%
   │   ├─> Длина входа: нормальная
   │   └─> Результат: False (не молчать)
   │
   ├─> Загружает промпты:
   │   │
   │   ├─> utils/randomness.random_prompt("core")
   │   │   └─> Возвращает: содержимое core.txt
   │   │
   │   └─> utils/randomness.random_prompt("ask")
   │       └─> Возвращает: содержимое ask.txt
   │
   ├─> Формирует запрос:
   │   │
   │   └─> system_prompt = core.txt + "\n\n" + ask.txt
   │       user_prompt = "Я не знаю, что делать дальше"
   │
   ├─> Вызывает LLM:
   │   │
   │   └─> call_openai(system_prompt, user_prompt)
   │       │
   │       └─> OpenAI API
   │           └─> Возвращает: "Попробуй подумать, что значит 
   │                            для тебя 'дальше'. Я понимаю 
   │                            твою растерянность."
   │
   ├─> Применяет фильтры:
   │   │
   │   ├─> filters.filter_advice(response)
   │   │   └─> Удаляет "Попробуй подумать"
   │   │       Результат: "что значит для тебя 'дальше'. 
   │   │                   Я понимаю твою растерянность."
   │   │
   │   ├─> filters.filter_empathy(response)
   │   │   └─> Удаляет "Я понимаю твою растерянность"
   │   │       Результат: "что значит для тебя 'дальше'."
   │   │
   │   └─> filters.filter_length(response, 200)
   │       └─> Длина OK
   │           Результат: "что значит для тебя 'дальше'."
   │
   └─> Возвращает: "что значит для тебя 'дальше'."
   │
   ▼
5. modules/ask.py
   │
   └─> Возвращает результат в mailn.py
   │
   ▼
6. mailn.py
   │
   ├─> utils/delay.random_delay(0.5, 2.0)
   │   └─> Пауза: 1.3 секунды
   │
   ├─> utils/delay.typing_effect(response)
   │   └─> Печатает посимвольно: "что значит для тебя 'дальше'."
   │
   └─> ВЫВОД: < что значит для тебя 'дальше'.
```

---

## 🎭 Сценарий с молчанием

```
1. ВВОД: "..."
   │
   ▼
2. mailn.py → modules/ask.py → responder.py
   │
   ▼
3. silence.should_be_silent("...", "ask")
   │
   ├─> Проверка паттернов: "..." in SILENCE_PATTERNS
   │   └─> True
   │
   ├─> Вероятность: 80%
   │
   └─> Результат: True (молчать)
   │
   ▼
4. responder.respond() возвращает: None
   │
   ▼
5. mailn.py
   │
   └─> if response: print(response)
       else: [молчание - ничего не выводится]
   │
   ▼
6. ВЫВОД: [пусто]
```

---

## 🌀 Сценарий искажения (режим distort)

```
1. ВВОД: "Я чувствую себя потерянным"
   │
   ▼
2. mailn.py → modules/distort.py
   │
   ▼
3. distort.py
   │
   ├─> Вызывает: responder.respond(input, mode="distort")
   │   │
   │   └─> LLM возвращает (с промптом distort.txt):
   │       "потерянным. отсутствие направления."
   │
   ├─> Выбирает случайный метод искажения:
   │   │
   │   └─> Варианты:
   │       • extract_random_word()
   │       • fragment_sentence()
   │       • reduce_text()
   │       • "..."
   │       • None
   │
   ├─> Выбрано: text.extract_random_word(response)
   │   │
   │   └─> Анализирует: "потерянным. отсутствие направления."
   │       Выбирает длинное слово: "отсутствие"
   │       Результат: "отсутствие"
   │
   └─> Возвращает: "отсутствие"
   │
   ▼
4. mailn.py
   │
   ├─> delay.random_delay()
   ├─> delay.typing_effect("отсутствие")
   │
   └─> ВЫВОД: < отсутствие
```

---

## 🔀 Переключение режимов

```
Состояние системы:
┌─────────────────────────┐
│   mode = "ask"          │  ← Текущий режим хранится в mailn.py
└─────────────────────────┘

Пользователь вводит: /distort
          ↓
mailn.py обрабатывает команду:
          ↓
if user_input.startswith("/"):
    command = user_input[1:]  # "distort"
    if command in modes:
        mode = command
          ↓
┌─────────────────────────┐
│   mode = "distort"      │  ← Режим изменен
└─────────────────────────┘
          ↓
Следующий ввод использует: modes["distort"](input)
```

---

## 🧩 Взаимодействие компонентов

### 1. Responder + Randomness + Config

```
responder.py                randomness.py              config/ai/
    │                            │                          │
    │  random_prompt("core")     │                          │
    ├───────────────────────────>│                          │
    │                            │  read_file(core.txt)     │
    │                            ├─────────────────────────>│
    │                            │                          │
    │                            │<─────────────────────────┤
    │                            │   "You are not an        │
    │                            │    assistant..."         │
    │<───────────────────────────┤                          │
    │   Промпт загружен          │                          │
```

### 2. Responder + Silence

```
responder.py              silence.py
    │                         │
    │  should_be_silent()     │
    ├────────────────────────>│
    │                         │
    │                         ├─ Проверка режима
    │                         ├─ Проверка паттернов
    │                         ├─ Расчет вероятности
    │                         ├─ random.random() < prob?
    │                         │
    │<────────────────────────┤
    │   True / False          │
    │                         │
    └─ Если True: return None │
       Если False: продолжить │
```

### 3. Responder + Filters

```
responder.py                      filters.py
    │                                 │
    │  LLM вернул:                    │
    │  "Попробуй подумать.            │
    │   Я понимаю тебя."              │
    │                                 │
    │  filter_advice(text)            │
    ├────────────────────────────────>│
    │                                 ├─ Поиск "попробуй"
    │                                 ├─ Удаление строки
    │<────────────────────────────────┤
    │  "Я понимаю тебя."              │
    │                                 │
    │  filter_empathy(text)           │
    ├────────────────────────────────>│
    │                                 ├─ Поиск "понимаю"
    │                                 ├─ Удаление фразы
    │<────────────────────────────────┤
    │  ""                             │
    │                                 │
    └─ Пустой результат → None        │
```

---

## 📦 Модульная структура

### Независимые модули (без внешних зависимостей)

```
┌─────────────────┐
│  filters.py     │  ← Чистые функции, работают с текстом
└─────────────────┘

┌─────────────────┐
│  text.py        │  ← Чистые функции, манипуляции со строками
└─────────────────┘
```

### Модули с минимальными зависимостями

```
┌─────────────────┐
│  delay.py       │  ← Зависит от: time, random, config.env
└─────────────────┘

┌─────────────────┐
│  silence.py     │  ← Зависит от: random, config.env
└─────────────────┘

┌─────────────────┐
│ randomness.py   │  ← Зависит от: config/ai/*.txt
└─────────────────┘
```

### Центральный модуль

```
┌─────────────────────────────────────┐
│          responder.py               │
│                                     │
│  Зависит от:                        │
│  • randomness.py (промпты)          │
│  • filters.py (очистка)             │
│  • silence.py (решения)             │
│  • config.env (API ключи)           │
│  • Внешние API (OpenAI/Anthropic)   │
└─────────────────────────────────────┘
```

### Режимы (используют responder)

```
┌─────────┐   ┌──────────┐   ┌──────┐   ┌─────────┐
│ ask.py  │   │distort.py│   │void.py│  │silence.py│
└────┬────┘   └────┬─────┘   └───┬──┘   └────┬────┘
     │             │              │           │
     └─────────────┴──────────────┴───────────┘
                       │
                       ▼
              ┌─────────────────┐
              │  responder.py   │
              └─────────────────┘
```

### Точка входа (использует все)

```
┌─────────────────────────────────────┐
│            mailn.py                 │
│                                     │
│  Зависит от:                        │
│  • modules/* (режимы)               │
│  • utils/delay.py (эффекты)         │
└─────────────────────────────────────┘
```

---

## 🔐 Управление конфигурацией

```
Запуск программы
      │
      ▼
┌─────────────────────────────────────┐
│  python-dotenv загружает            │
│  src/config/config.env              │
│                                     │
│  OPENAI_API_KEY=sk-...              │
│  LLM_PROVIDER=openai                │
│  SILENCE_PROBABILITY=0.2            │
│  ...                                │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│  os.getenv("OPENAI_API_KEY")        │
│  os.getenv("SILENCE_PROBABILITY")   │
│  ...                                │
└──────────────┬──────────────────────┘
               │
               ▼
    Используется во всех модулях:
    • responder.py (API ключи)
    • silence.py (вероятности)
    • delay.py (задержки)
```

---

## 🚦 Жизненный цикл запроса

```
START
  │
  ├─> [1] Пользователь вводит текст
  │        ↓
  ├─> [2] Проверка команды (/, /quit, /help)
  │        ↓ если команда
  │        └─> Обработка команды → GOTO [1]
  │        ↓ если текст
  ├─> [3] Определение режима (ask/distort/void/silence)
  │        ↓
  ├─> [4] Вызов модуля режима
  │        ↓
  ├─> [5] responder.respond()
  │        ↓
  │     [5.1] Проверка молчания
  │        ↓ если молчать
  │        └─> return None → GOTO [9]
  │        ↓ если говорить
  │     [5.2] Загрузка промптов
  │        ↓
  │     [5.3] Вызов LLM API
  │        ↓ если ошибка
  │        └─> return None → GOTO [9]
  │        ↓ если успех
  │     [5.4] Применение фильтров
  │        ↓
  │     [5.5] Возврат результата
  │        ↓
  ├─> [6] Для distort: дополнительная обработка
  │        ↓
  ├─> [7] Возврат в mailn.py
  │        ↓
  ├─> [8] Если ответ не None:
  │        ├─> Пауза (delay)
  │        ├─> Эффект печати (typing)
  │        └─> Вывод
  │        ↓
  ├─> [9] Если ответ None:
  │        └─> Молчание (ничего не выводится)
  │        ↓
  └─> [10] GOTO [1] (новый цикл)
```

---

## 🧠 Принятие решений

### Дерево решений для молчания

```
Входной текст
     │
     ├─> Режим = "silence"? ──┐
     │   └─> ДА → МОЛЧАТЬ     │
     │   └─> НЕТ              │
     │       ↓                 │
     ├─> Текст в SILENCE_PATTERNS ("...", ".")? ──┐
     │   └─> ДА → 80% МОЛЧАТЬ │                   │
     │   └─> НЕТ              │                   │
     │       ↓                 │                   │
     ├─> Длина текста < 5?    │                   │
     │   └─> ДА → prob × 1.5  │                   │
     │   └─> НЕТ → prob × 1.0 │                   │
     │       ↓                 │                   │
     ├─> Режим multiplier:    │                   │
     │   • ask: ×1.0          │                   │
     │   • distort: ×1.5      │                   │
     │   • void: ×4.0         │                   │
     │       ↓                 │                   │
     ├─> Финальная вероятность│                   │
     │   = base × multiplier  │                   │
     │       ↓                 │                   │
     └─> random() < prob?     │                   │
         └─> ДА → МОЛЧАТЬ ────┴───────────────────┘
         └─> НЕТ → ГОВОРИТЬ
```

---

## 🎨 Паттерны проектирования

### 1. Strategy Pattern (Стратегия) - Режимы

```
┌──────────────────────────┐
│  ModeStrategy (abstract) │
│                          │
│  + execute(input): str   │
└────────────┬─────────────┘
             │
     ┌───────┴────────┬─────────┬─────────┐
     │                │         │         │
┌────▼────┐   ┌───────▼──┐  ┌──▼───┐  ┌─▼──────┐
│  Ask    │   │ Distort  │  │ Void │  │Silence │
│Strategy │   │ Strategy │  │Strat │  │Strategy│
└─────────┘   └──────────┘  └──────┘  └────────┘

Использование:
modes = {
    "ask": ask_strategy,
    "distort": distort_strategy
}
result = modes[current_mode].execute(input)
```

### 2. Pipeline Pattern (Конвейер) - Фильтры

```
Input → filter_advice() → filter_empathy() → filter_length() → Output

Каждый фильтр:
• Принимает текст
• Модифицирует текст
• Возвращает текст
• Может быть добавлен/удален независимо
```

### 3. Facade Pattern (Фасад) - Responder

```
┌────────────────────────────────────────┐
│         Responder (Facade)             │
│                                        │
│  Скрывает сложность:                   │
│  • Загрузки промптов                   │
│  • Вызова API                          │
│  • Применения фильтров                 │
│  • Логики молчания                     │
│                                        │
│  Предоставляет простой интерфейс:      │
│  respond(input, mode) → Optional[str]  │
└────────────────────────────────────────┘
```

### 4. Dependency Injection (Внедрение зависимостей)

```
# Конфигурация через env файл
config.env → os.getenv() → используется в модулях

# Промпты через файлы
config/ai/*.txt → randomness.py → используется в responder

# API провайдер через настройки
LLM_PROVIDER=openai → call_openai()
LLM_PROVIDER=anthropic → call_anthropic()
```

---

## 🔄 Масштабируемость

### Добавление нового режима

```
1. Создать файл: src/modules/new_mode.py
   
   def new_mode(user_input: str) -> Optional[str]:
       return respond(user_input, mode="new_mode")

2. Создать промпт: src/config/ai/new_mode.txt
   
   [описание поведения режима]

3. Добавить в mailn.py:
   
   from src.modules.new_mode import new_mode
   
   modes = {
       ...
       "new_mode": new_mode
   }

Готово! Новый режим работает.
```

### Добавление нового фильтра

```
1. Добавить функцию в filters.py:
   
   def filter_questions(text: str) -> str:
       # удаляет вопросы
       return modified_text

2. Добавить в responder.py:
   
   from src.ai.filters import filter_questions
   
   filtered = filter_advice(raw)
   filtered = filter_empathy(filtered)
   filtered = filter_questions(filtered)  # новый фильтр
   filtered = filter_length(filtered, max_len)

Готово! Фильтр применяется.
```

### Добавление нового LLM провайдера

```
1. Добавить функцию в responder.py:
   
   def call_new_provider(system_prompt, user_prompt):
       # логика вызова API
       return response

2. Обновить call_llm():
   
   if LLM_PROVIDER == "new_provider":
       return call_new_provider(system, user)

3. Настроить в config.env:
   
   LLM_PROVIDER=new_provider
   NEW_PROVIDER_API_KEY=...

Готово! Новый провайдер доступен.
```

---

## 🧪 Тестируемость

### Unit тесты (изолированные)

```
filters.py:
  ✓ test_filter_advice_removes_try()
  ✓ test_filter_empathy_removes_understand()
  ✓ test_filter_length_truncates()

silence.py:
  ✓ test_should_be_silent_with_ellipsis()
  ✓ test_should_be_silent_probability()
  ✓ test_silence_mode_always_silent()

text.py:
  ✓ test_extract_random_word()
  ✓ test_fragment_sentence()
  ✓ test_reduce_text()

randomness.py:
  ✓ test_random_prompt_loads_file()
  ✓ test_safe_format_with_kwargs()
```

### Integration тесты (с моками)

```
responder.py:
  ✓ test_respond_with_mock_llm()
  ✓ test_respond_applies_filters()
  ✓ test_respond_respects_silence()

modules/ask.py:
  ✓ test_ask_calls_responder_with_ask_mode()

mailn.py:
  ✓ test_command_parsing()
  ✓ test_mode_switching()
```

---

## 📊 Метрики производительности

```
Типичный запрос:
┌──────────────────────────────┬──────────┐
│ Этап                         │ Время    │
├──────────────────────────────┼──────────┤
│ Ввод пользователя            │ 0ms      │
│ Проверка молчания            │ <1ms     │
│ Загрузка промптов            │ 1-5ms    │
│ Вызов LLM API                │ 500-3000ms│
│ Применение фильтров          │ 1-3ms    │
│ Искажение текста (distort)   │ 1-2ms    │
│ Пауза перед ответом          │ 500-2000ms│
│ Эффект печати (30 симв/сек)  │ 300-1000ms│
├──────────────────────────────┼──────────┤
│ ИТОГО                        │ 1.3-6s   │
└──────────────────────────────┴──────────┘
```

---

## 🛡️ Обработка ошибок

```
Ошибка на уровне:

API (OpenAI/Anthropic):
  → try/except в call_llm()
  → return None
  → mailn.py → молчание

Файловая система (промпты):
  → try/except в randomness.py
  → return ""
  → responder → использует пустой промпт или дефолтный