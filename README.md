# <img src="readme_resource/logo_name.jpeg" width="100%"/>

**Star_Void** - терминальный софт на Python.
Он не повышает продуктивность, не лечит и не мотивирует.

Это программа для паузы.

> A terminal space where answers are optional

---

## Концепция

Пользователь взаимодействует с программой через терминал.

**ИИ:**
- Отвечает кратко (1-2 предложения)
- Избегает советов
- Не утешает
- Допускает молчание
- Иногда отвечает уклончиво или фрагментарно

ИИ здесь - не личность.
Он - среда.

---

## Запуск

### Для слабых ПК (рекомендуется)
Используйте Docker с умной моделью Gemma 2:

1. Убедитесь, что Docker и Docker Compose установлены
2. Запустите сервисы:
   ```bash
   cd docker && docker-compose up -d
   ```
3. Установите модель:
   ```bash
   docker exec -it ollama-smart ollama pull gemma2
   ```
4. Запустите приложение:
   ```bash
   docker exec -it star-void-app python main.py
   ```
   
   Или используйте скрипт:
   ```bash
   ./scripts/start_docker.sh
   ```

### Локальный запуск
1. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```

2. Убедитесь, что Ollama запущен:
   ```bash
   ollama serve
   ```

3. Установите модель:
   ```bash
   ollama pull gemma2
   ```

4. Запустите приложение:
   ```bash
   python main.py
   ```

---

## 👤 Автор

**@StanislavBTC**

---

## 💭 Философия

> Это программа для паузы, не для продуктивности.
> Молчание - это тоже ответ.
> Отсутствие - это тоже присутствие.

**Star_Void** не решает проблемы. Он создаёт пространство.

# !!!
Егор, если ты это читаешь, то не думай, что я этот проект пишу через ИИ

Я накинул скелет перед отъездом, а когда вернусть буду постпенно САМ всё доделывать